{% extends 'base.html.twig' %}

{% block title %}TypeIssue{% endblock %}

{% block body %}
    <h1>TypeIssue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_issue.id }}</td>
            </tr>
            <tr>
                <th>Nom_type_issue</th>
                <td>{{ type_issue.nomTypeIssue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_issue_index') }}">back to list</a>

    <a href="{{ path('type_issue_edit', {'id': type_issue.id}) }}">edit</a>

    {{ include('type_issue/_delete_form.html.twig') }}
{% endblock %}
