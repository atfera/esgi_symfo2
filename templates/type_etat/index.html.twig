{% extends 'base.html.twig' %}

{% block title %}TypeEtat index{% endblock %}

{% block body %}
    <h1>TypeEtat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_type_etat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_etat in type_etats %}
            <tr>
                <td>{{ type_etat.id }}</td>
                <td>{{ type_etat.nomTypeEtat }}</td>
                <td>
                    <a href="{{ path('type_etat_show', {'id': type_etat.id}) }}">show</a>
                    <a href="{{ path('type_etat_edit', {'id': type_etat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_etat_new') }}">Create new</a>
{% endblock %}
