{% extends 'base.html.twig' %}

{% block title %}TypeEtat{% endblock %}

{% block body %}
    <h1>TypeEtat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_etat.id }}</td>
            </tr>
            <tr>
                <th>Nom_type_etat</th>
                <td>{{ type_etat.nomTypeEtat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_etat_index') }}">back to list</a>

    <a href="{{ path('type_etat_edit', {'id': type_etat.id}) }}">edit</a>

    {{ include('type_etat/_delete_form.html.twig') }}
{% endblock %}
