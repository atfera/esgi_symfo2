<form method="post" action="{{ path('assoc_issue_notation_delete', {'id': assoc_issue_notation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assoc_issue_notation.id) }}">
    <button class="btn">Delete</button>
</form>
